---
title: "2026-08-07"
date: 2026-08-07
summary: "Added experimental MOQ live streaming support with configurable relay and broadcast settings across environments"
tags: ["Audit", "Cases", "Recordings", "Live view", "Organisations", "Projects", "Workflows", "Notifications"]
draft: false
generated: true
---

<!-- Generated from the matching GitOps release. Review public wording before merge. -->

## Hub Frontend

- Added experimental MOQ live streaming support with configurable relay and broadcast settings across environments
- Introduced a synchronised multi-camera player with shared timeline playhead, playback controls, and seek support
- Upgraded the media timeline with inline video previews, thumbnail strips, adjustable media bar sizing, and improved tooltip interactions
- Improved timeline usability with alternating row styling, enhanced resize controls, and clearer transport/time selection layout
- Refined motion detection settings by enforcing minimum sensitivity values and removing the ability to disable detection with a zero threshold
- Fixed legacy camera motion-threshold handling to preserve recording behavior after upgrades
- Added development debugging improvements with stricter source map resolution settings for VS Code launches

## Hub API

- Routed case media redaction through the workflow engine with seeded workflow runs, dynamic stage queue resolution, and improved storage credential handling
- Added media projections for `eventNames`, `categoryNames`, and `markerSummary` in v20260101 media reads
- Preserved and forwarded TUS `Upload-Metadata` headers, including FPS metadata, during upload proxying

## Hub Workflows

- Enforced authoritative organisation scoping for media-patch ingestion to prevent workers from re-scoping existing recordings via forged envelope data
- Added validation to reject media-patch results when no persisted workflow run with a valid organisation scope exists
- Improved ingest reliability documentation and handling for non-transactional sink writes and retry semantics
- Added tests covering organisation scope resolution, media-patch validation, and permanent sink failure handling
- Updated ingest dependency to v1.0.3 to denormalise `markerSummary` for ingested markers

## Pipeline Event

- Fixed pipeline event decoding to support object-shaped `user.audit` payloads
- Upgraded to Go 1.25 and refreshed project dependencies

## Pipeline Monitor

- Updated models dependency to v1.7.3/v1.7.4, including a fix for audit DLQ issues

## Pipeline Sequence

- Upgraded `models` dependency from v1.7.3 to v1.7.7, including fixes for audit DLQ handling
- Improved media processing by preserving FPS precision during sequence handling

## Pipeline Notification

- Updated `models` dependency to v1.7.3 to resolve audit DLQ issues
- Upgraded `models` to v1.7.7 with fixes for preserving media FPS precision
- Bumped `markers` dependency to v1.2.4

## Pipeline Analysis

- Added persistence of media FPS metadata from thumbnail analysis as a fallback when upstream FPS data is missing
- Implemented validation and precision-preserving handling for FPS values, rejecting invalid, NaN, infinite, and out-of-range data
- Updated thumbnail processing flow to extract and store FPS information from analysis events
- Added race-safe database updates to prevent overwriting existing authoritative FPS metadata

## Pipeline Thumbnail

- Added ffprobe-based FPS detection and propagation so downstream analysis can persist media frame-rate metadata when upstream FPS is unavailable
- Improved FPS parsing to preserve precise fractional frame rates and prefer `avg_frame_rate` over nominal rates for better media accuracy
- Added comprehensive tests covering ffprobe frame-rate parsing, fractional precision handling, and fallback behavior
- Updated thumbnail generation pipeline to return and forward detected source FPS alongside generated thumbnails
- Upgraded models dependency to v1.7.3 to resolve audit DLQ issues

## Pipeline Sprite

- Updated models dependency to v1.7.3 to resolve audit DLQ issues

## Pipeline Export

- Updated dependency models to v1.7.3 to resolve audit DLQ issues
- Upgraded models dependency to v1.7.4 for latest fixes and improvements

## Pipeline Redaction

- Added support for redaction-surface workflows that inject user-reviewed regions directly into workflow runs
- Improved region resolution by supporting inline editor tracks, upstream detection results, and seeded detection snapshots
- Enhanced workflow integration with deterministic stage processing, lifecycle event publishing, and workflow result propagation
- Simplified and clarified deployment/configuration by consolidating queue handling and documenting shared Vault/provider behavior
- Expanded processing documentation around OpenCV rendering, FFmpeg audio remuxing, and per-frame redaction behavior

## Vault

- Added support for agent-provided recording FPS metadata during media uploads, with MP4 parsing retained as a fallback
- Preserved fractional FPS precision across media parsing and pipeline payload generation instead of rounding to integers
- Added validation and handling for FPS metadata, including bounds checks and invalid value filtering
- Extended TUS upload routing to propagate FPS metadata from upload headers and metadata fields
- Added comprehensive tests covering FPS parsing, metadata preference logic, routing behavior, and fractional FPS persistence
